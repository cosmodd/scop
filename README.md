# 🧊 scop

A simple 3D object viewer implemented using C++ and OpenGL.

## 🚀 Running

The project is built on MacOS so it may not work on other platforms. To run the project, simply run the following command:
```bash
make run
```